export const metadata = {
  title: 'AI 초벌 교정 시스템',
  description: 'PDF 문서 AI 교정 도구',
}

export default function RootLayout({ children }) {
  return (
    <html lang="ko">
      <body>{children}</body>
    </html>
  )
}
